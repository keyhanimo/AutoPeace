kind = "web"
previewPath = "/"
title = "AutoPeace"
version = "1.0.0"
id = "artifacts/autopeace"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21795

[services.development]
run = "pnpm --filter @workspace/autopeace run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/autopeace", "run", "build" ]
publicDir = "artifacts/autopeace/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21795"
BASE_PATH = "/"
